server:
  port: 8081

spring:
  application:
    name: customer-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/customers_db  # URL de conexión
  config:
    import: "optional:configserver:"
